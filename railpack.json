{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": [
        { "cmd": "echo 'relay: no build step, runs from source via tsx'" }
      ]
    }
  }
}
